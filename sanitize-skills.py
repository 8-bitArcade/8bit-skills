#!/usr/bin/env bash
# sanitize.py — Called by sync-skills-repo.sh to strip private data from skills.
# Replaces hardcoded values with {{TEMPLATE}} placeholders.
# This way the repo contains only sanitized, platform-ready skills.
